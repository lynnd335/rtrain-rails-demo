$("#post-modal").html("<%= escape_javascript(render 'new') %>");
$("#post-modal").show();
$("#post-modal").draggable().resizable({ handles: "n, e, s, w, se, sw, nw, ne" });
$(".ajax-form-button").click(function(){
	$("#post-modal").hide();
})