<% @posts.order('id DESC').each do |post| %>
  <tr>
    
    <td><%= post.subject %></td>
			<td><%= post.content %></td>

    <td><%= link_to "Edit", edit_post_path(post), remote: true%></td>
    <td><%= link_to "Delete", post, method: :delete, remote: true%></td>

  </tr>
<% end %>